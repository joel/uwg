<h1>New Post</h1>

<%= form_with(model: @input, method: :post, url: posts_path) do |form| %>
  <%= render 'form', post: @input, form: form %>

  <div class="actions">
    <%= form.submit "Create Post" %>
  </div>
<% end %>

<%= link_to 'Back', posts_path %>
